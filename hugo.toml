baseURL = 'https://julianferrone.com'
languageCode = 'en-us'
title = 'Julian Ferrone'

[taxonomies]
tag = "tags"
series = "series"

[outputs]
home = ["HTML"]
section = ["HTML"]
taxonomy = []      # disables /tags/foo/ etc.

[markup]
  [markup.highlight]
    style = 'trac'